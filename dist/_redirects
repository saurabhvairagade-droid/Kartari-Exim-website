# Redirect netlify.app subdomain to primary domain (prevents duplicate content in Google)
https://kartariexim.netlify.app/*  https://kartariexim.com/:splat  301!

# SPA fallback - serve index.html for all unmatched routes
/*  /index.html  200
